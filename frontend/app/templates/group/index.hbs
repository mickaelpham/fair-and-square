<h3>{{model.name}}</h3>
<p>{{model.description}}</p>

<div class="grid">
  <div class="col sml-12">
    {{link-to 'Edit group' 'group.edit' model class="button"}}
  </div>
</div>

<h4>Last expenses</h4>

{{expense-list expenses=model.expenses}}

<div class="grid">
  <div class="col sml-12">
    {{link-to 'Add expense' 'group.add-expense' model class="button blue"}}
  </div>
</div>
